# 用户管理页面问题排查指南

## 问题描述
用户管理页面显示"暂无用户数据"，无法正常显示用户列表。

## 可能原因及解决方案

### 1. 云函数未部署
**问题**：`userManagement` 云函数未正确部署到云端。

**解决方案**：
1. 在微信开发者工具中，右键点击 `cloudfunctions/userManagement` 文件夹
2. 选择"上传并部署：云端安装依赖"
3. 等待部署完成

### 2. 数据库集合不存在
**问题**：`users` 或 `userPermissions` 集合不存在。

**解决方案**：
1. 在微信开发者工具中，打开"云开发"控制台
2. 进入"数据库"页面
3. 检查是否存在 `users` 集合
4. 如果不存在，需要先有用户登录创建数据

### 3. 用户数据为空
**问题**：数据库中确实没有用户数据。

**解决方案**：
1. 确保至少有一个用户已经登录过系统
2. 用户登录时会自动在 `users` 集合中创建记录

### 4. 权限问题
**问题**：云函数没有访问数据库的权限。

**解决方案**：
1. 检查云函数的环境ID是否正确
2. 确保云函数有数据库读写权限

## 调试步骤

### 步骤1：测试云函数
1. 打开测试页面：`pages/test-launch/index`
2. 点击"测试用户管理云函数"按钮
3. 查看测试结果和错误信息

### 步骤2：检查控制台日志
1. 在微信开发者工具中打开"调试器"
2. 查看控制台输出的详细错误信息
3. 特别关注云函数调用的返回结果

### 步骤3：检查数据库
1. 打开云开发控制台
2. 查看 `users` 集合是否有数据
3. 查看 `userPermissions` 集合是否有数据

## 常见错误及解决方法

### 错误1：FunctionName parameter could not be found
**原因**：云函数未部署
**解决**：重新部署 `userManagement` 云函数

### 错误2：Environment not found
**原因**：云开发环境ID配置错误
**解决**：检查 `app.js` 中的环境ID配置

### 错误3：Permission denied
**原因**：云函数权限不足
**解决**：检查云函数权限配置

### 错误4：Collection not found
**原因**：数据库集合不存在
**解决**：先让用户登录创建数据，或手动创建集合

## 测试数据创建

如果需要测试数据，可以：

1. **创建测试用户**：
   - 在测试页面点击"模拟登录"
   - 这会创建一个测试用户记录

2. **手动创建权限记录**：
   ```javascript
   // 在云开发控制台的数据库页面执行
   db.collection('userPermissions').add({
     data: {
       openId: 'test-openid',
       role: 'normal',
       createTime: new Date(),
       updateTime: new Date()
     }
   })
   ```

## 验证修复

修复后，应该能看到：
1. 用户管理页面显示用户列表
2. 可以搜索和查看用户详情
3. 可以修改用户角色

## 联系支持

如果问题仍然存在，请提供：
1. 控制台错误日志
2. 云函数调用结果
3. 数据库集合截图
4. 具体的错误信息
