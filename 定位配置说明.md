# 聚会相机定位功能配置说明

## 功能概述

聚会相机已集成定位功能，拍照后会自动获取当前位置信息并显示在照片上。

## ⚠️ 重要提醒

根据[微信小程序官方文档](https://developers.weixin.qq.com/miniprogram/dev/api/location/wx.getLocation.html)：

> **从 2.17.0 起 `wx.getLocation` 增加调用频率限制**

为了保证用户体验和遵守平台规范，本实现仅在**拍照成功后**调用一次定位，避免频繁调用。

## 配置步骤

### 1. 申请腾讯位置服务密钥（Key）

1. 访问腾讯位置服务官网：https://lbs.qq.com/
2. 注册/登录账号
3. 进入控制台 -> 应用管理 -> 我的应用
4. 点击"创建应用"
5. 填写应用名称（如：聚会相机小程序）
6. 创建成功后，点击"添加Key"
7. **选择"WebServiceAPI"类型**（重要！）
8. 复制生成的Key

### 2. 配置密钥

打开文件：`miniprogram/pages/collects/party-camera/index.js`

找到第 221 行，将 `YOUR_TENCENT_MAP_KEY` 替换为你申请的密钥：

```javascript
reverseGeocoding(latitude, longitude) {
  const that = this;
  
  // 腾讯位置服务密钥（需要在 https://lbs.qq.com/ 申请）
  const TENCENT_MAP_KEY = 'YOUR_TENCENT_MAP_KEY'; // 替换为你的密钥
  
  // 如果没有配置密钥，只显示经纬度
  if (TENCENT_MAP_KEY === 'YOUR_TENCENT_MAP_KEY') {
    // ...将显示经纬度坐标
  }
  // ...
}
```

**提示**：如果不配置密钥，定位功能仍然可用，但只会显示经纬度坐标而非具体地址。

### 3. 配置小程序域名白名单

在微信公众平台的小程序后台配置服务器域名：

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入"开发" -> "开发管理" -> "开发设置" -> "服务器域名"
3. 在 **request合法域名** 中添加：
   ```
   https://apis.map.qq.com
   ```

## 功能特性

### 自动获取定位

- **触发时机**：拍照成功后自动触发（遵循微信 2.17.0+ 频率限制）
- **坐标系统**：使用 `gcj02`（国测局坐标系），适用于国内地图服务
- **定位精度**：开启高精度定位，超时时间 5 秒
- **权限请求**：首次使用时会请求定位权限
- **用户选择**：用户可以选择"授权"或"跳过"

### 定位信息展示

定位信息优先级（从高到低）：
1. **POI 信息**：具体地点名称（如：星巴克咖啡、北京大学）
2. **推荐地址**：格式化的详细地址
3. **标准地址**：区 + 街道形式
4. **经纬度**：定位成功但解析失败时显示坐标
5. **未知位置**：定位失败时显示

### 位置信息显示位置

- **相框下方**：实时显示位置信息，带有位置图标 📍
- **分享图片**：位置信息会被绘制到分享图片的水印中

### 隐私保护

- 用户可以拒绝定位权限，不影响其他功能使用
- 位置信息仅用于照片记录，不会上传到服务器
- 所有数据都保存在本地

## 权限说明

已在 `app.json` 中配置的权限：

```json
{
  "permission": {
    "scope.userLocation": {
      "desc": "需要您的授权以获取位置信息，记录拍照地点"
    }
  },
  "requiredPrivateInfos": [
    "getLocation"
  ]
}
```

## 故障排查

### 1. 定位一直失败

**可能原因**：
- 手机定位服务未开启
- 小程序定位权限未授权
- 网络连接问题

**解决方案**：
1. 检查手机系统定位服务是否开启
2. 在小程序中重新授权定位权限
3. 检查网络连接状态

### 2. 显示经纬度而不是地址

**可能原因**：
- 腾讯地图 Key 未配置或配置错误
- 服务器域名未添加到白名单
- 腾讯地图 API 调用失败

**解决方案**：
1. 检查 Key 是否正确配置
2. 确认域名白名单配置正确
3. 查看控制台是否有错误信息

### 3. "未知位置"提示

**可能原因**：
- 用户拒绝了定位授权
- GPS 信号弱
- 定位服务异常

**解决方案**：
1. 引导用户重新授权
2. 移动到信号较好的位置
3. 重启小程序重试

## API 调用限制

腾讯位置服务 API 调用限制：
- **免费配额**：每日 10,000 次
- **并发限制**：5 次/秒

建议：
- 仅在拍照后调用，不要频繁调用
- 可以考虑缓存最近的位置信息，短时间内重复拍照使用缓存

## 注意事项

### 📱 平台相关

1. **iOS 系统**：首次请求定位权限时，需要在 `app.json` 中配置 `permission` 说明文案（已配置）
2. **Android 系统**：部分设备可能需要用户手动开启定位服务
3. **开发工具**：工具中定位使用 IP 定位，可能有误差，且仅支持 gcj02 坐标

### ⚡ 性能与限制

4. **调用频率限制**：微信从 2.17.0 起对 `wx.getLocation` 增加调用频率限制，[详见公告](https://developers.weixin.qq.com/community/develop/doc/000a02f2c5026891650e7f40351c01)
5. **高精度定位**：`isHighAccuracy: true` 需要设置 `highAccuracyExpireTime >= 3000ms` 才有效
6. **坐标系选择**：
   - `wgs84`：GPS 原始坐标
   - `gcj02`：国测局坐标，适用于国内（推荐）

### 🔒 隐私合规

7. **隐私声明**：在小程序隐私设置中需要声明使用定位功能的用途
8. **权限说明**：已在 `app.json` 中配置 `scope.userLocation` 和 `requiredPrivateInfos`
9. **用户控制**：用户可随时拒绝定位，不影响拍照等其他功能

### 🧪 测试建议

10. **真机测试**：必须在真机上测试，开发工具无法准确模拟 GPS 定位
11. **测试环境**：室外定位精度更高，室内可能依赖 WiFi/基站定位

## 成本估算

基于腾讯位置服务的免费配额：
- 每日最多 10,000 次免费调用
- 假设平均每个用户每天拍照 10 次
- 可支持约 1,000 个活跃用户/天
- 超出配额后可按需购买套餐

## 类目要求

根据微信官方文档，使用定位功能需要小程序属于特定类目。请确保您的小程序类目符合要求：

### 适用类目示例

- **工具类**：天气、信息查询、办公、与地理位置相关的打卡服务等
- **生活服务**：家政、餐饮、外卖等
- **旅游服务**：景区服务、住宿服务等
- **社交类**：照片打印、拍摄美化等

详细类目列表请参考：[wx.getLocation 开放类目](https://developers.weixin.qq.com/miniprogram/dev/api/location/wx.getLocation.html)

**建议**：在微信公众平台的小程序管理后台，检查您的服务类目是否支持定位功能。

## API 参数说明

根据微信官方文档，`wx.getLocation` 主要参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| type | string | wgs84 | 坐标系类型：`wgs84`（GPS）或 `gcj02`（国测局） |
| altitude | boolean | false | 是否返回高度信息 |
| isHighAccuracy | boolean | false | 开启高精度定位 |
| highAccuracyExpireTime | number | - | 高精度定位超时时间（ms），≥3000ms 有效 |

**当前配置**：
```javascript
wx.getLocation({
  type: 'gcj02',              // 国测局坐标系
  isHighAccuracy: true,        // 开启高精度
  highAccuracyExpireTime: 5000 // 超时 5 秒
})
```

## 更新日志

- **2025-11-02**：添加定位功能，支持拍照后自动获取位置信息
  - 使用 `gcj02` 坐标系
  - 开启高精度定位
  - 符合微信 2.17.0+ 频率限制规范
  - 完善错误提示和降级策略

