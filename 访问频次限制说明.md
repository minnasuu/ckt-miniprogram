# 答案之书 - 访问频次限制功能说明

## 功能概述

为小程序添加了完善的访问频次限制功能，防止用户过度使用，保护服务器资源和AI调用成本。

## 限制策略

### 1. 每日次数限制
- **经典版**：每天限制 10 次
- **AI 版**：每天限制 5 次
- 每日 0 点自动重置计数

### 2. 时间间隔限制
- 两次提问之间最小间隔：3 秒
- 防止用户快速连续提问

### 3. 独立计数
- 经典版和 AI 版分别计数
- 切换模式不影响各自的使用次数

## 用户体验

### 1. 实时提示
- 界面底部显示"今日剩余：X 次"
- 剩余次数 ≤ 2 时，获取答案后会弹出提示

### 2. 友好提示
- 达到每日限制时，显示详细说明弹窗
- AI 版用完后，提示可切换到经典版继续使用
- 时间间隔不足时，显示需要等待的秒数

### 3. 数据持久化
- 使用微信小程序本地存储（`wx.setStorageSync`）
- 数据按日期存储，自动识别新的一天

## 配置参数

在 `index.js` 文件顶部可以调整以下参数：

```javascript
const FREQUENCY_CONFIG = {
  DAILY_LIMIT: 10,        // 经典版每日限制次数
  AI_DAILY_LIMIT: 5,      // AI版每日限制次数
  INTERVAL_LIMIT: 3000,   // 两次提问最小间隔（毫秒）
};
```

### 调整建议
- **增加限制**：适用于资源紧张或成本控制
- **减少限制**：适用于推广期或VIP用户
- **时间间隔**：建议保持在 2-5 秒之间

## 技术实现

### 核心方法

1. **`initFrequencyLimit()`** - 初始化频次数据
   - 页面加载时调用
   - 读取本地存储的使用记录
   - 自动识别新的一天并重置计数

2. **`checkCanQuery()`** - 检查是否可以提问
   - 检查时间间隔
   - 检查每日次数限制
   - 返回 true/false

3. **`updateUsageCount()`** - 更新使用次数
   - 成功获取答案后调用
   - 更新内存和本地存储
   - 显示剩余次数提示

### 数据存储结构

```javascript
// 经典版存储
{
  date: "2025-10-27",  // 日期
  count: 3             // 已使用次数
}

// AI版存储
{
  date: "2025-10-27",
  count: 2
}
```

## 使用场景

### 场景 1：正常使用
1. 用户输入问题并回车
2. 系统检查是否可以提问
3. 通过检查后获取答案
4. 更新使用次数
5. 显示剩余次数

### 场景 2：达到每日限制
1. 用户输入问题并回车
2. 系统检查发现已达限制
3. 显示友好提示弹窗
4. AI版可提示切换到经典版

### 场景 3：时间间隔不足
1. 用户快速连续提问
2. 系统检查时间间隔不足
3. 显示需要等待的秒数
4. 用户等待后可继续提问

## 扩展建议

### 1. VIP 功能
可以添加 VIP 用户识别，给予更高的限制：

```javascript
// 在 data 中添加
isVIP: false,
vipDailyLimit: 50,
vipAIDailyLimit: 20,

// 在 checkCanQuery 中判断
const limit = this.data.isVIP ? this.data.vipDailyLimit : this.data.dailyLimit;
```

### 2. 服务器端验证
对于重要场景，建议添加服务器端验证：

```javascript
// 调用云函数验证
wx.cloud.callFunction({
  name: 'checkFrequency',
  data: { userId: xxx }
})
```

### 3. 统计分析
可以添加使用统计，分析用户行为：

```javascript
// 记录每次使用
wx.cloud.database().collection('usage_logs').add({
  data: {
    userId: xxx,
    mode: 'ai',
    timestamp: Date.now()
  }
})
```

## 注意事项

1. **本地存储限制**
   - 小程序本地存储有 10MB 限制
   - 当前方案占用空间极小，无需担心

2. **用户清除数据**
   - 用户可以清除小程序数据重置计数
   - 如需严格控制，建议结合服务器端验证

3. **时区问题**
   - 使用本地时间判断日期
   - 跨时区用户可能出现偏差

4. **性能影响**
   - 本地存储读写性能优秀
   - 对用户体验无明显影响

## 测试建议

1. **正常流程测试**
   - 连续提问，验证计数是否正确
   - 切换模式，验证独立计数

2. **边界测试**
   - 达到限制时的提示
   - 时间间隔不足的提示
   - 跨天重置功能

3. **异常测试**
   - 清除小程序数据后重新进入
   - 快速切换模式并提问

## 更新日志

### v1.0.0 (2025-10-27)
- ✅ 实现每日次数限制
- ✅ 实现时间间隔限制
- ✅ 经典版和AI版独立计数
- ✅ 实时显示剩余次数
- ✅ 友好的用户提示
- ✅ 数据持久化存储
- ✅ 可配置的参数设置
