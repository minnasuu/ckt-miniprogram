# 字体未生效问题调试指南

## 🔧 已修复的问题

### 1. **使用云文件临时链接**
之前直接使用 `cloud://` 协议可能在某些情况下无法加载，现在改为：
1. 先通过 `wx.cloud.getTempFileURL` 获取临时 HTTPS 链接
2. 再使用临时链接加载字体

### 2. **Canvas 绘制前确保字体已加载**
在 `generateImage` 方法中添加了 `loadFontForCanvas` 回调，确保字体加载完成后再绘制

### 3. **添加 global: true 参数**
使字体在全局范围内可用

---

## 📋 调试步骤

### 步骤 1: 检查控制台日志

重新编译小程序后，在控制台查看以下日志：

#### ✅ 成功的日志
```
字体临时链接: https://636c-cloud1-8gzjqovx9c2ec2e9-1330048123.tcb.qcloud.la/Momozhuanji.ttf?sign=...
全局字体 Momozhuanji 加载成功
Canvas 字体加载成功
```

#### ❌ 失败的日志
```
获取云文件临时链接失败
全局字体 Momozhuanji 加载失败
```

### 步骤 2: 验证云存储文件

1. 打开微信开发者工具
2. 点击"云开发" → "云存储"
3. 确认 `Momozhuanji.ttf` 文件存在
4. 检查文件路径是否为根目录
5. 确认权限设置为"所有用户可读"

### 步骤 3: 测试字体加载

在控制台手动执行以下代码测试：

```javascript
// 测试获取临时链接
wx.cloud.getTempFileURL({
  fileList: ['cloud://cloud1-8gzjqovx9c2ec2e9.636c-cloud1-8gzjqovx9c2ec2e9-1330048123/Momozhuanji.ttf'],
  success: (res) => {
    console.log('临时链接:', res.fileList[0].tempFileURL);
  },
  fail: (err) => {
    console.error('获取失败:', err);
  }
});
```

### 步骤 4: 测试字体渲染

在 ai-answer-book 页面：
1. 输入问题并获取答案
2. 点击拍照或分享按钮
3. 查看生成的图片中文字是否使用了 Momozhuanji 字体

---

## 🐛 常见问题排查

### 问题 1: 获取临时链接失败

**可能原因：**
- 云环境 ID 不正确
- 文件路径不正确
- 云开发未初始化

**解决方法：**
```javascript
// 在 app.js 的 onLaunch 中确认云环境已初始化
wx.cloud.init({
  env: "cloud1-8gzjqovx9c2ec2e9",
  traceUser: true,
});
```

### 问题 2: 字体加载成功但未显示

**可能原因：**
- 字体文件损坏
- 字体名称不匹配
- Canvas 绘制时机问题

**解决方法：**
1. 重新上传字体文件
2. 确认字体族名称为 `Momozhuanji`
3. 在绘制前添加延迟：
```javascript
setTimeout(() => {
  this.drawCanvas(canvas, ctx, action);
}, 100);
```

### 问题 3: 真机上字体未生效

**可能原因：**
- 临时链接过期
- 网络问题
- 真机字体加载限制

**解决方法：**
1. 清除缓存重新编译
2. 检查真机网络连接
3. 使用永久 HTTPS 链接（推荐）

---

## 🚀 推荐方案：使用永久 HTTPS 链接

### 方法 1: 获取云存储永久链接

1. 在云存储中找到 `Momozhuanji.ttf`
2. 右键 → "获取临时链接"
3. 复制链接（去掉 `?sign=` 后面的部分）
4. 更新代码中的链接

### 方法 2: 使用 CDN

将字体文件上传到 CDN，使用 CDN 链接：

```javascript
wx.loadFontFace({
  family: 'Momozhuanji',
  source: 'url("https://your-cdn.com/Momozhuanji.ttf")',
  global: true,
  // ...
});
```

---

## 📝 验证字体是否生效的方法

### 方法 1: 查看生成的图片
生成图片后，仔细观察文字的字形，对比系统默认字体

### 方法 2: 使用不同的文字测试
输入一些特殊字符，看是否能正确显示

### 方法 3: 在控制台检查字体
```javascript
// 在 Canvas 绘制后检查
console.log(ctx.font); // 应该显示 "42px Momozhuanji"
```

---

## 🔍 当前配置信息

- **云环境 ID**: cloud1-8gzjqovx9c2ec2e9
- **云文件 ID**: cloud://cloud1-8gzjqovx9c2ec2e9.636c-cloud1-8gzjqovx9c2ec2e9-1330048123/Momozhuanji.ttf
- **字体族名**: Momozhuanji
- **加载方式**: 临时链接 + wx.loadFontFace
- **使用位置**: 
  - app.js (全局加载)
  - pages/collects/ai-answer-book/index.js (Canvas 绘制前加载)

---

## ✅ 检查清单

- [ ] 字体文件已上传到云存储
- [ ] 文件权限设置为"所有用户可读"
- [ ] 控制台显示"字体临时链接"
- [ ] 控制台显示"字体加载成功"
- [ ] 重新编译小程序
- [ ] 清除缓存后测试
- [ ] 真机测试

---

## 💡 下一步建议

如果以上方法都无效，请：

1. **检查字体文件本身**
   - 确认 Momozhuanji.ttf 是有效的字体文件
   - 尝试在电脑上安装该字体测试

2. **简化测试**
   - 创建一个简单的测试页面
   - 只测试字体加载，不涉及其他功能

3. **查看详细错误信息**
   - 在 fail 回调中打印完整的 err 对象
   - 截图控制台所有相关日志

4. **联系技术支持**
   - 提供云环境 ID
   - 提供完整的错误日志
   - 提供字体文件信息
