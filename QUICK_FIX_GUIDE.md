# 快速解决用户管理页面问题

## 当前问题
`Error: 获取用户列表失败: collection.get:fail -502005 database collection not exists.`

## 立即解决步骤

### 步骤1：重新部署云函数
1. 在微信开发者工具中，右键点击 `cloudfunctions/userManagement` 文件夹
2. 选择"上传并部署：云端安装依赖"
3. 等待部署完成

### 步骤2：创建测试数据
**方法A：使用测试页面（推荐）**
1. 访问 `pages/test-launch/index` 页面
2. 点击"创建测试用户"按钮
3. 等待创建完成

**方法B：手动创建数据库集合**
1. 打开云开发控制台
2. 进入"数据库"页面
3. 创建 `users` 集合（权限：仅创建者可读写）
4. 创建 `userPermissions` 集合（权限：仅创建者可读写）

### 步骤3：验证功能
1. 返回用户管理页面
2. 应该能看到用户列表或提示信息
3. 如果显示"数据库集合不存在"提示，点击"去创建"按钮

## 预期结果

### 成功情况
- 用户管理页面显示用户列表
- 可以搜索和查看用户详情
- 可以修改用户角色

### 测试数据
创建测试用户后，你将看到：
- **测试用户1**：普通用户
- **测试用户2**：高级用户
- **测试用户3**：管理员

## 故障排除

### 如果云函数部署失败
1. 检查云开发环境是否正常
2. 确认环境ID配置正确
3. 检查网络连接
4. 重试部署

### 如果测试用户创建失败
1. 检查云函数是否已部署
2. 查看控制台错误信息
3. 手动创建数据库集合
4. 重新尝试创建测试用户

### 如果仍然显示错误
1. 清除小程序缓存
2. 重新编译项目
3. 检查云开发控制台中的云函数状态
4. 查看详细的错误日志

## 联系支持

如果问题仍然存在，请提供：
1. 控制台错误日志
2. 云函数部署状态截图
3. 数据库集合列表截图
4. 具体的错误信息

## 技术说明

这个错误是因为：
1. 数据库集合 `users` 和 `userPermissions` 不存在
2. 云函数尝试访问不存在的集合
3. 需要先创建集合或让用户登录创建数据

修复后，系统会：
1. 优雅处理集合不存在的情况
2. 提供创建测试数据的选项
3. 显示友好的提示信息
