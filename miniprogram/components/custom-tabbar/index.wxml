<view class="tab-bar">
  <view class="tab-bar-border"></view>
  <view wx:for="{{list}}" wx:key="index" class="tab-bar-item {{selected === index ? 'active' : ''}} {{item.text === 'CKT'?'ckt':''}}" data-index="{{index}}" bindtap="switchTab">
    {{item.text}}
  </view>
</view>