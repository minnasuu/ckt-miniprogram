<view class="checkbox-container {{class}}" catch:tap="handleChange">
  <view class="checkbox {{checked ? 'checked' : ''}}">
    <view class="checkbox-inner" wx:if="{{checked}}"></view>
  </view>
  <view class="checkbox-text">{{text}}</view>
</view>